# E2E (End-to-End) Tests

Playwright を用いたE2Eの自動テスト環境です。
フロントエンドのUIコンポーネントのみならず、バックエンドAPIとの結合を含めたアプリケーション全体の振る舞いを検証します。

## 実行方法

プロジェクトルートの `test_e2e.ps1` を実行してください。自動でサーバーを起動し、テストが完了するとレポートを出力して終了します。
